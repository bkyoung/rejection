import css from 'styled-jsx/css';

export default css`
    .scoreContainer {
      display: flex;
      flex-shrink: 1;
    }

    .score {
      margin-top: 0;
      margin-bottom: 0;
      font-weight: 900;
      letter-spacing: -0.025em;
    }
`;
